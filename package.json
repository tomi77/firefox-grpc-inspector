{
  "name": "firefox-grpc-inspector",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "protobufjs": "^7.0.0"
  }
}
