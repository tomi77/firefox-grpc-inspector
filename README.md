# Firefox gRPC Inspector

Firefox DevTools extension that decodes gRPC-Web traffic.

## Install (development)

1. `npm install && npm run build`
2. Firefox → `about:debugging` → This Firefox → Load Temporary Add-on → `dist/manifest.json`

## Development

`npm run build:watch`

## Tests

`npm test`
